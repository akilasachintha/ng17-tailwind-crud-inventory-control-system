export interface IProduct {
  productId?: string;
  categoryId?: string;
  categoryName: string;
  productName: string;
  productDescription: string;
  productPrice: number;
  productQuantity: number;
}
